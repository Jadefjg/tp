
# 数据逻辑
所有的数据都是以项目做隔离，项目的创建者和成员才能看到该项目下的数据。
# 仪表盘
预留，用于展示相关数据图表。

# 项目管理 -> 项目管理
## 新建
权限只开放了管理员，点击【新建项目】按钮,填入名称和描述点击确定即可
## 分配
项目成员，项目创建者和管理员可以分配项目给相应的人员，这样项目相关的所有数据都可以共享协作完成。  
1. 点击项目后的分配按钮
2. 输入姓名，模糊搜索，勾选相应的成员后（支持多选），点击【确定】
## 删除
点击删除，之后确认。删除是逻辑删除，如果误操作删除了，请联系管理员（王瑞）
## 编辑
点击项目后的编辑按钮，修改名称和描述点击【确定】即可
## 通知配置
该配置为在此项目下的所有任务完成后会有钉钉群通知：  
1. access_token和secret为群机器人的对应字段。
2. 勾选all全部,会at所有人；未勾选可以添加需要@的群成员钉钉绑定手机号码。  
3. 添加时需要点击手机号输入框后的添加按钮添加到列表中之后确定才会生效
4. 模板为通知消息的模板，格式为markdown格式，其中的占位符会在测试任务执行之后替换为真实运行的结果

# 项目管理 -> 环境管理
## 新建
点击【新建环境】按钮，选择项目，填入名称，描述后【确定】。
## 编辑
点击环境后的【编辑】按钮，修改名称描述后点击【确定】
## 详情
点击环境名称会跳转到详情页。
### 导出
点击【导出】按钮，导出格式为json
### 详情编辑
点击编辑按钮，可以进入编辑模式。__注意：在编辑状态下，只有点击了【保存】按钮，数据才会保存到后端__
#### 增加环境变量
在键，值，注释中填入相应的值之后点击【添加】按钮；__环境变量的值都会尝试用json加载，如果需要表达的是数字字符串,需要加双引号,eg: `"123"`，`true`会加载为`True`  
环境变量支持嵌套，可以在值中输入`${xxx}`来引用另一个环境变量, 如果xxx不存在会原样输出。  
环境变量中有`http_`开头的一组环境变量，用来初始化会话对象，在键的框中输入http_会自动加载出来供选。  
[参见文档](https://docs.aiohttp.org/en/stable/client_reference.html#aiohttp.ClientSession)
#### 删除
点击环境变量的【删除】按钮可以删除某条键值对
#### 编辑
点击环境变量的【编辑】按钮，该条记录会填充到添加表单中，同时从列表中删除，修改完后再次点击【添加】按钮即可
#### 导入
点击导入按钮，选择文件，点击确定。导入的文件格式和导出的格式一样，并且会自动的去掉相应的id值。修改导入的文件需要先删除已选择的文件。
#### 取消
点击【取消】按钮会放弃所有修改恢复到最初的状态
## 复制
如果想要复制已经有的环境，可以点击环境后的【复制】按钮，修改相关信息后点击【确定】按钮，复制会连同环境详情一同复制，这样可以方便切换环境来执行
## 删除
点击【删除】按钮，确认后可以删除，如果误操作删除，请联系管理员。
# 接口管理 -> 接口列表
主要用于管理接口信息
## 新建接口
目前未开放，只能通过yapi新建然后同步
## 接口详情
点击接口标题链接，会跳转到接口详情，目前未开发
## 禁用/启用
禁用的接口在接口测试中不会自动填充

# 接口管理 -> 同步配置
此处添加的同步配置会在每天00:00执行同步，将yapi中的相应接口同步到平台中
## 新建配置
点击【新建配置】按钮，弹出对话框中填写或者选择相应的信息。
其中url为yapi的服务地址，远程项目名称为yapi上的项目名称，远程项目ID为yapi上的项目ID，远程项目Token为yapi上的项目token,项目和远程的项目是1对多的关系，点击确定
## 禁用配置
配置禁用后不会同步

# 用例管理 -> 用例管理
## 新建用例
点击【新建用例】按钮,选择项目，输入标题，选择优先级和标签，添加描述，点击确定。系统中不存在的标签，回车后会添加，保存后会新建该标签。  
__用例的优先级和标签用于在任务中添加用例时检索用例使用，请认真填写.__  
## 导出用例
输入检索条件和勾选可以限制导出用例的范围，导出为json格式
## 导入用例
点击【导入用例】按钮之后，先选择项目，之后选择导入的文件。导入的文件格式同导出，并且会自动的处理id信息。
## 删除用例
点击用例后的【删除】按钮，确认后用例会删除。
## 编辑用例
点击用例后的【编辑】按钮，之后同新建
## 用例详情
点击用例标题链接，可以跳转到用例详情
### 测试数据
测试数据为测试中使用的数据，作用域为单个用例，测试数据有多行时，每行数据会执行一次用例
#### 测试数据编辑
点击【测试数据】右上角的编辑按钮或者页面下方的【编辑数据】按钮，进入数据编辑模式
#### 增加行
#### 增加列
#### 删除列
点击列名上的叉叉可以删掉该列。
#### 编辑列
点击某行数据后的【编辑】按钮，可以行内编辑，点击行中的【保存】可以保存编辑的数据，但是未提交到后台
#### 删除行
#### 保存数据
点击【测试数据】右上角的【保存】按钮，提交数据到后台
### 测试步骤
测试步骤包含了用例里面的测试步骤，目前有3种类型：接口，python脚本，宏。
1. 接口：发送http请求，然后提取，校验
2. python脚本：执行一段python代码
3. 宏： 执行一段预置的python代码，复用性强的代码，如登陆之类的
#### 编辑步骤
点击【编辑步骤】后，测试步骤变成编辑模式，在编辑模式下可以：
1. 新建步骤
2. 插入步骤
3. 编辑步骤
4. 步骤排序
5. 删除步骤
##### 新建步骤
1. 点击最下面的【新建步骤】按钮
2. 填入标题，选择类别，填写注释，然后根据类别填写对应的信息，之后点击【确定】按钮
###### 接口
接口的表单同postman，若项目有同步过来接口，在地址栏输入路径前缀会检索出来，对应的接口，选择接口会自动填充请求方法，请求体请求头等数据。  
接口表单中可以是使用${xxx}的方式来引用变量，变量查找顺序为
1. 当前测试数据（包含前面步骤提取的值，这些值会覆盖掉同名的测试数据）
2. 设置的全局变量（怎么设置全局变量后面会说到）
3. 执行时选择的环境中的环境变量
地址栏支持带前缀检索，例如：
```
${url}/abc // 会检索出来,在该项目下路径以为/abc前缀的接口，列表中会显示 ${url}/abc/xxx 等
http://abc.com/def // 会检索出来,在该项目下路径以为/def前缀的接口，列表中会显示 http://abc.com/def/xxx 等

```
__提取和校验填写后需要勾选，勾选时会校验必填项__    
__校验和提取中的`jsonpath`只是`jsonpath`的一个子集实现，只支持用`.`取属性或者下标。__
__提取到的值的只在当前的测试用例中有效。__
校验的value会按照如下步骤解析：
1. 如果校验的value完全匹配${xxx}的格式，那就在当前用例的环境变量中去找，找到就直接返回这个值；如果环境变量找不到这个值，那就尝试`json`加载，`ast.literal_eval`加载，如果都不能加载就原样输出
2. 如果校验的value中含有${xxx}的格式，那就取出xxx然后在环境变量中查找，能找到就做字符串替换;找不到就原样输出
3. 如果校验的value不含有${xxx}的格式，那就尝试`json`加载，`ast.literal_eval`加载，如果都不能加载就原样输出
所以:  
1. __如果需要校验的值是数字型字符串一定要加双引号，写成`"123"`形式, `123`会认为是整数；字符串`abc`写成哪种形式都可以__
2. __支持`[1, 2, '3']`或者`${[1, 2, 3]}`表示一个列表__
3. __支持`{'a': 123}`表示一个字典__

###### python脚本
执行一段python脚本，脚本需要勾选是否是协程，怎么判断是否需要勾选协程？
1. 这段代码中是否有IO操作？所谓IO就是有网络连接，磁盘读写，数据库操作, sleep操作等。如果有，是否使用了异步协程？如果使用了异步协程库就会有async/await 关键字，这里需要勾选协程，不勾选会报语法错误无法执行；如果没有使用协程库，即没有使用async/await关键字，那么不要勾选协程，这种情况下勾选协程如果有长时间的IO操作会卡住整个系统。
2. 如果无IO操作,也没有使用async/await 关键字，可以勾选协程，也可以不勾选
3. __使用的async/await 关键字，不需要勾选协程__
4. 勾选了协程会在主线程中异步执行，不勾选协程使用线程池的方式异步执行。
python脚本中可以使用`ctx`变量，这个变量记录了当前执行过程中的上下文，包括环境变量，接口客户端，日志等信息。
具体实现见类`testrunner.context.Context`
常见用法：
```python
logger = ctx.get_logger() # 日志记录会记录到当前的报告中
logger.info('this is a info')
ctx.set_global('foo', 'bar') # 把全局环境变量foo设置为bar，如果当前的用例中有这个数据，这个数据不会被覆盖；也就是说不会影响局部的环境变量
client = ctx.get_client() # 获取会话实例 aiohttp.ClientSession实例
async with client.get(url) as resp:
    await resp.json()  # 到请求的返回内容
ctx.set_client(client) # 设置会话实例
# Context类继承于collections.ChainMap（类字典）,有类似于字典的用法
ctx.get('foo')  # 环境变量中获取foo, 没有找到返回None
ctx['foo'] # 环境变量中获取foo，没有找到抛出KeyError
ctx.wrap_string('${foo}') # 获取环境变量foo的值
# ctx.eval(xx)尝试json.loads, ast.literal_eval加载，都加载不了返回原样
ctx.eval("foo") # foo
ctx.eval("[1, 2, 3]") # 列表[1, 2, 3]
ctx.eval("'123'") # 字符串123
ctx.get_http_config() # 获取环境变量中的http参数
# __mock_开头，__结尾的是mock方法
# phone 手机号
# today yyyy:mm:dd 日期
# tomorrow 第二天日期
# ......
# 参见 testrunner.context.BuiltinMock
ctx.get('__mock_phone__') # 随机手机号码
```
##### 宏
宏是预置的一段python脚本，与python脚本的执行方式相同。
#### 插入步骤
在任何步骤前点击【插入步骤】按钮，就可以在该步骤前插入一条步骤。
#### 编辑步骤
在任何步骤前点击【编辑】按钮
#### 删除
在任何步骤前点击【删除】按钮，然后确认，这里删除后无法恢复请谨慎操作
#### 步骤排序
有时候可能测试的步骤添加失误需要重新调整，在编辑模式下，拖动就可以了，拖动步骤会在每次拖动结束时提交到后台
### 调试
TODO
### 返回
返回到上个页面, 这里的返回是从哪里跳转过来的就返回到哪里。




宏需要管理员审核才能选中。宏修改后不会影响已经选择宏的用例，需要更新宏需要到用例中重新选择宏保存。宏修改后会自动